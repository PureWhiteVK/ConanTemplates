{
    "configurations": [
      {
        "type": "lldb",
        "request": "launch",
        "name": "Launch CMake Target",
        "program": "${command:cmake.launchTargetPath}",
        "args": [],
        "cwd": "${workspaceFolder}"
      }
    ],
  }